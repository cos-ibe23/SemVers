meta {
  name: Get Profile
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/v1/auth/profile
  body: none
  auth: bearer
}

auth:bearer {
  token: {{shipperToken}}
}

docs {
  Get the current user's shipper profile.

  Returns 404 if user has not completed onboarding.
}
