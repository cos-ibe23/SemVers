meta {
  name: Delete Pickup Request
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/v1/pickup-requests/:id
  body: none
  auth: bearer
}

params:path {
  id: 1
}

auth:bearer {
  token: {{shipperToken}}
}

docs {
  Delete/cancel a pickup request.

  Path Parameters:
  - id: The pickup request ID

  Note: Cannot delete a request with status CONVERTED.

  Returns 404 if not found or doesn't belong to the shipper.
}
