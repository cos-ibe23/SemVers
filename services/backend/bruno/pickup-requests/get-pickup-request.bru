meta {
  name: Get Pickup Request
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/v1/pickup-requests/:id
  body: none
  auth: bearer
}

params:path {
  id: 1
}

auth:bearer {
  token: {{shipperToken}}
}

docs {
  Get a specific pickup request by ID.

  Path Parameters:
  - id: The pickup request ID

  Returns 404 if not found or doesn't belong to the shipper.
}
